% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modify_ae_header.R
\name{modify_ae_header}
\alias{modify_ae_header}
\alias{modify_ae_spanning_header}
\title{Modify AE Table Headers and Spanning Headers}
\usage{
modify_ae_header(x, ..., text_interpret = c("md", "html"))

modify_ae_spanning_header(x, ..., text_interpret = c("md", "html"))
}
\arguments{
\item{x}{object of class "tbl_ae", "tbl_ae_count", or "tbl_ae_focus"}

\item{...}{Updates to header and spanning headers. Can pass either named
arguments (e.g. \code{label = "**AE**"}) or a
selecting formula (e.g. \code{all_ae_cols() ~ "**Grade {by}**"})}

\item{text_interpret}{String indicates whether text will be interpreted with
\code{\link[gt:md]{gt::md()}} or \code{\link[gt:html]{gt::html()}}. Must be \code{"md"} (default) or \code{"html"}.}
}
\value{
an object of the same class as the input
}
\description{
Modify AE Table Headers and Spanning Headers
}
\section{Details}{

When updating the header and spanning headers, the following dynamic values
are available. The dynamic values are inserted using \code{stringr::str_glue()}
syntax.
\itemize{
\item \code{"{by}"} the level of the by variable
\item \code{"{strata}"} the level of the strata variable
\item \code{"{n}"} number of patients within a stratum
\item \code{"{N}"} total number of patients
\item \code{"{p}"} proportion of patients within a stratum (on the zero-one scale)
}

The \code{"{n}"}, \code{"{N}"}, and \code{"{p}"} statistics are not available for
\code{tbl_ae_count()} objects.
}

\section{Example Output}{

\if{html}{Example 1}

\if{html}{\figure{modify_ae_header_ex1.png}{options: width=65\%}}
}

\examples{
modify_ae_header_ex1 <-
  df_adverse_events \%>\%
  tbl_ae(
    id = patient_id,
    ae = adverse_event,
    soc = system_organ_class,
    by = grade,
    strata = trt
  ) \%>\%
  modify_ae_header(all_ae_cols() ~ "**Grade {by}**") \%>\%
  modify_ae_spanning_header(all_ae_cols() ~ "**{strata}** N = {n} / {N}")
}
