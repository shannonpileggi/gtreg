% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_overall.R
\name{add_overall_tbl_ae}
\alias{add_overall_tbl_ae}
\alias{add_overall.tbl_ae}
\alias{add_overall.tbl_ae_count}
\alias{add_overall.tbl_ae_focus}
\title{Tabulate Overall Summary}
\usage{
\method{add_overall}{tbl_ae}(x, type = c("both", "by", "strata"), ...)

\method{add_overall}{tbl_ae_count}(x, type = c("both", "by", "strata"), ...)

\method{add_overall}{tbl_ae_focus}(x, type = c("both", "by", "strata"), ...)
}
\arguments{
\item{x}{Object of class \code{"tbl_ae"}, \code{"tbl_ae_focus"}, or \code{"tbl_ae_count"}}

\item{type}{Specify the type of overall statistics to include.
\itemize{
\item \code{"by"} adds summaries across the \verb{by=} levels
\item \code{"strata"} adds summaries across the \verb{strata=} levels
\item \code{"both"} adds summaries across both the \verb{by=} and \verb{strata=} levels
Default it \code{"both"}
}}

\item{...}{Not used}

\item{by}{logical indicating whether the \verb{by=} argument should be
included in the overall table. Default is \code{FALSE}}

\item{strata}{logical indicating whether the \verb{strata=} argument should be
included in the overall table. Default is \code{FALSE}}
}
\value{
Summary object of class \code{"tbl_ae"}
}
\description{
Tabulate Overall Summary
}
\section{Notes}{

If the spanning headers are modified prior to the call of \code{add_overall()},
the ordering of the columns may not be correct.
}

\section{Example Output}{

\if{html}{Example 1}

\if{html}{\figure{add_overall_ex1.png}{options: width=70\%}}

\if{html}{Example 2}

\if{html}{\figure{add_overall_ex2.png}{options: width=70\%}}

\if{html}{Example 3}

\if{html}{\figure{add_overall_ex3.png}{options: width=70\%}}
}

\examples{
# Example 1 -----------------------------------------------------------------
add_overall_ex1 <-
  df_adverse_events \%>\%
  tbl_ae(
    id = patient_id,
    ae = adverse_event,
    soc = system_organ_class,
    by = grade,
    header = "**Grade {level}**"
  ) \%>\%
  add_overall(type = 'by') \%>\%
  bold_labels()

# Example 2 -----------------------------------------------------------------
add_overall_ex2 <-
  df_adverse_events \%>\%
  tbl_ae_count(
    ae = adverse_event,
    soc = system_organ_class,
    by = grade,
    strata = trt,
    header = "**Grade {level}**"
  ) \%>\%
  add_overall() \%>\%
  bold_labels()

# Example 3 -----------------------------------------------------------------
add_overall_ex3 <-
  df_adverse_events \%>\%
  tbl_ae_focus(
    id = patient_id,
    include = c(any_complication, grade3_complication),
    ae = adverse_event,
    strata = trt
  ) \%>\%
  add_overall(type = 'strata')

}
